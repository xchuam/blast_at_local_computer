# Examples

This directory is intended to be tracked in git.

* `input/` contains small input files used by the notebook and legacy examples.
* `output/` contains small, curated output examples from test downloads.

Large local downloads, BLAST databases, and extracted sequence outputs should be reviewed before committing.
